#include <stdio.h>
#include "holberton.h"

/**
 * _strlen - return length of a string.
 *
 *@s: String.
 *
 *
 *
 * Return: int.
 */

int _strlen(char *s)
{
	int a;
	/*Repeat loop until you meet the null char*/
	for (a = 0; s[a] != '\0'; a++)
	{
	}
	return (a);
}
